const person = 'adam vai';
const person2 = "Ben vai";
const person3 = `Trump vai`;

const multiline = 'first line' + 'second line' + 'third line'

const newMultiline = `First line
Second line
Third line`
// console.log(newMultiline);

const a = 20;
const b = 30;
const summary = 'sum of: ' + a + ' and ' + b + ' is: ' + (a + b);

const newSummary = `sum of ${a} and ${b} is: ${a + b}`
console.log(newSummary);

